# Express CRUD Practice
A simple REST API built with Node.js and Express, implementing CRUD operations for user management.

## Endpoints
- GET /users - Get all users
- POST /users - Create a user
- PUT /users/:id - Update a user
- DELETE /users/:id - Delete a user

## Run locally
npm install
npm start
